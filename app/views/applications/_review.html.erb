		<div class="application">
			<h4><a href="<%= artist_path(app.artist) %>">
					<%= app.artist.try(:full_name) %>
			</a></h4>

			<div class="submissions">
				<%= render partial: "submissions/submissions_module", :locals => { :object => app } %>
			</div>

		</div>
